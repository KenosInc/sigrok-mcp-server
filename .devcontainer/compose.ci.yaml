services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/sigrok-mcp-server:cached
      - sigrok-mcp-server-go-mod-cache:/go/pkg/mod
    command: sleep infinity

volumes:
  sigrok-mcp-server-go-mod-cache:
